import { Logger } from '../../models/logger';
export declare function useLoggerFactory(apiRef: any, { logger, logLevel }: {
    logger?: Logger;
    logLevel?: string | false;
}): void;
export declare function useLogger(name: string): Logger;
